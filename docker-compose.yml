services:
  app:
    build: .
    container_name: nextpropconnect-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3015:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:9SI1P68dxVQPHFDNUGxzSK77pCFKwkEQ@supabase-db:5432/propconnect
      - AUTH_SECRET=WEuiqTPybSi991cDRh2HPE/yonL7eltPlKXS5aNV+Z0=
      - AUTH_TRUST_HOST=true
      - NEXTAUTH_URL=https://nextpropconnect.co.za
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      # Google OAuth
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      # Ozow Payments
      - OZOW_SITE_CODE=${OZOW_SITE_CODE}
      - OZOW_PRIVATE_KEY=${OZOW_PRIVATE_KEY}
      - OZOW_API_KEY=${OZOW_API_KEY}
      - OZOW_TEST_MODE=${OZOW_TEST_MODE:-true}
      # MsgHub Configuration (centralized messaging)
      - MSGHUB_URL=https://messaging.itedia.co.za
      - MSGHUB_API_KEY=mh_gGswkMdesqHOcIC7Y-qERhF2EVM0CMiPqXnXTdMXsWI
      - MSGHUB_TENANT_ID=1c01d2ea-782a-4b36-b83a-1d116e3d8e77
      - MSGHUB_WEBHOOK_SECRET=wx3CEKnzUZ4Xq6AFXSPQNOl0f4E8w3Mg
      # Email Configuration (SMTP)
      - SMTP_HOST=mail.itedia.co.za
      - SMTP_PORT=587
      - SMTP_USER=${SMTP_USER:-}
      - SMTP_PASS=${SMTP_PASS:-}
      - EMAIL_FROM=NextPropConnect <noreply@nextpropconnect.co.za>
      # AI Configuration (OpenAI)
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    networks:
      - travelinn-edge
      - travelinn-network
      - supabase_supabase-internal

networks:
  travelinn-edge:
    external: true
  travelinn-network:
    external: true
  supabase_supabase-internal:
    external: true
